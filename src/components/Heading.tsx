import React from 'react';
import styled from '@emotion/styled';

const StyledHeading = styled.h1`
  font-size: 32px;
`;

const Heading = () => <StyledHeading className="bg-red-500">Daniel Navarrete</StyledHeading>;

export default Heading;
// 